# LLM Service Selection
# Options: openai, bedrock, ollama
LLM_SERVICE=openai

# OpenAI Configuration (required if LLM_SERVICE=openai)
OPENAI_API_KEY=your-openai-api-key-here

# AWS Bedrock Configuration (required if LLM_SERVICE=bedrock)
# Option 1: Use AWS SSO Profile
# AWS_PROFILE=default

# Option 2: Use AWS Access Keys (alternative to AWS_PROFILE)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_REGION=us-east-1

# Ollama Configuration (required if LLM_SERVICE=ollama)
# OLLAMA_HOST=http://localhost:11434
